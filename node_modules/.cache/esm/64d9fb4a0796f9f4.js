"main";let Joi;_079‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);let jwt;_079‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);let UserInputError;_079‍.w("apollo-server-express",[["UserInputError",["UserInputError"],function(v){UserInputError=v}]]);let signUp,signIn;_079‍.w("../schemas",[["signUp",["signUp"],function(v){signUp=v}],["signIn",["signIn"],function(v){signIn=v}]]);let JWT_EMAIL;_079‍.w("./config",[["JWT_EMAIL",["JWT_EMAIL"],function(v){JWT_EMAIL=v}]]);let User,Message,Order,Cart;_079‍.w("../models",[["User",["User"],function(v){User=v}],["Message",["Message"],function(v){Message=v}],["Order",["Order"],function(v){Order=v}],["Cart",["Cart"],function(v){Cart=v}]]);let transporter;_079‍.w("../mail",[["transporter",["transporter"],function(v){transporter=v}]]);let Auth;_079‍.w("../auth",[["*",null,function(v){Auth=v}]]);












_079‍.d({
  User: {
    messages: async ({ id }, args, { req }, info) => {
      Auth.checkSignedIn(req)
      const userMessage = await Message.find({ userId: id })
      return userMessage.map((x) => ({
        id: `${x._id}`,
        userId: x.userId,
        title: x.title,
        body: x.body,
        isOpened: x.isOpened
      }))
    },
    orders: async ({ id }, args, { req }, info) => {
      Auth.checkSignedIn(req)
      const userOrder = await Order.find({ customerId: id })
      return userOrder.map((x) => ({
        id: `${x._id}`,
        customerId: x.customerId,
        orderStatusId: x.orderStatusId,
        orderTypeId: x.orderTypeId,
        datePlaced: x.datePlaced,
        datePickUp: x.datePickUp,
        isOpened: x.isOpened
      }))
    },
    cart: async ({ id }, args, context, info) => {
      const userCart = await Cart.find({ userId: id })
      return userCart.map((x) => ({
        id: `${x.id}`,
        productId: x.productId,
        quantity: x.quantity,
        userId: x.userId
      }))
    }

  },
  Query: {
    me: async (root, args, { req }, info) => {
      // projection
      Auth.checkSignedIn(req)
      const meData = await User.findById(req.session.userId)
      return {
        id: `${meData._id}`,
        name: meData.name,
        email: meData.email,
        mobile: meData.mobile
      }
    },
    users: (root, args, { req }, info) => {
      // TODO: auth, projection, pagination

      Auth.checkSignedIn(req)

      return User.find({})
    }

  },
  Mutation: {
    signUp: async (root, args, { req }, info) => {
      // TODO: not auth, validation

      Auth.checkSignedOut(req)

      await Joi.validate(args, signUp, { abortEarly: false })

      const user = await User.create(args)

      const token = await jwt.sign({
        email: user.email,
        JWT_EMAIL
      }, {
        expiresIn: '1d'
      })

      _079‍.g.console.log(token)
      const mailOptions = {
        from: MAIL_ADDRESS,
        to: finalUser.email,
        subject: 'Your Order(s) has succesfully processed.',
        html: '<p>Thank your for ordering with us</p>'
      }
      await transporter.sendMail(mailOptions, (err, info) => {
        (err) ? new UserInputError('501 error') : _079‍.g.console.log(info)
      })

      return user
    },
    signIn: async (root, args, { req }, info) => {
      const { userId } = req.session

      // Auth.checkSignedOut(req)
      if (userId) {
        return User.findById(userId)
      }
      await Joi.validate(args, signIn, { abortEarly: false })

      const user = await Auth.attemptSignIn(args.email, args.password)

      req.session.userId = user.id

      return user
    },
    signOut: (root, args, { req, res }, info) => {
      Auth.checkSignedIn(req)

      return Auth.signOut(req, res)
    },
    confirmEmail: async (root, args, context, info) => {

    }
  }
});
