"main";let gql;_b41‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

_b41‍.d(gql`
    extend type Query {
        message: [Message!]!
    }
    extend type Mutation { 
        deleteMessage(id: ID) : Boolean
        addMessage(userId: ID, title: String, body: String): Message
    }

    type Message {
        id: ID!
        userId: ID!
        title: String!
        body: String!
        isOpened: Boolean
    }
`);
